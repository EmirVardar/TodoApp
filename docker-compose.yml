services:
  java_db:
    container_name: java_db
    image: postgres:16
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: Deneme
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: {}

